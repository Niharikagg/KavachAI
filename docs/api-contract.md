# API Contract

API endpoints will be documented here as they are implemented.

## Contextual Indicators

The `contextual_indicators` field uses these exact string values:

- `village_level_location`
- `exact_age`
- `exact_date`
- `sensitive_health_attribute`
- `uniqueness_word`
